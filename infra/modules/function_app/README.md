# module: function_app

The processor: Flex Consumption Function App, VNet-integrated, Managed Identity only, no public inbound (the trigger pulls). One Event Hubs trigger per hub via HUBS_CONFIG; reads Redis/Key Vault/bundle by identity.
